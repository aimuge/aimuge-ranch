# 艾牧戈智慧牧场 · 修改指南（从哪儿改）

> **唯一的修改位置**（源码）：
> `/Users/sun/Documents/Codex/2026-08-16/new-chat-3/outputs/aimuge-ranch/`
> ⚠️ 不要改 `/Users/Shared/aimuge-ranch`（那是运行副本，改源码后会自动同步）

## 一、90% 的内容都在 `js/data.js`（数据都在这里）

| 想改什么 | 在 data.js 里找这一块 | 举例 |
|---|---|---|
| 牧场名称/位置/面积 | `meta:` | `name:'艾牧戈家庭牧场'` |
| 牛/羊/马/驼 存栏数 | `species:` | `count:8540` → 改成 `count:9000` |
| 今日天气/温度 | `weather:` | `temp:-22` |
| 导航栏目名（左侧菜单） | `nav:` | `title:'养殖管理'` |
| 草场分类/地块 | `grasslandTypes:` `grassland:` | 面积、载畜量 |
| 饲草库存 | `forageInventory:` | `stock:420` |
| 屠宰记录/出栏计划 | `slaughterRecords:` `slaughterPlans:` | |
| 防疫记录/免疫程序 | `vaccineRecords:` `vaccinePlans:` | |
| 智慧装备清单 | `deviceList:` | 名称、数量、协议 |
| 产品库存 | `productInventory:` | |
| 牧游产品/订单 | `tourism:` | |
| 政务对接系统 | `gov:` | |
| 牧事日志/账本/用工/保险/安防 | `logs:` `ledger:` `workers:` `insurances:` `safety:` | |

**改法示例**（把羊从 8540 改成 9000）：
```js
{ key:'sheep', name:'羊', emoji:'🐑', color:'#8a9a5b', breed:'呼伦贝尔羊（短尾羊）', count:8540,   ← 改成 9000
```
> 改数字/文字直接改，**注意别删掉逗号和引号**，改完保存即可。

## 二、页面布局/文字说明 → `js/app.js`
- 每个页面的标题、介绍文字、卡片内容都在 `js/app.js` 里（搜页面对应函数，如 `pageLivestock`）。
- 一般用户不需要动这里，除非要改"页面长什么样/写什么话"。

## 三、颜色/草原风格 → `css/style.css`
- 顶部 `:root` 里的颜色变量（绿色 `--green`、沙金 `--sand` 等）。
- 改主题色就改这里。

## 四、首页结构/浏览器标签名 → `index.html`
- `<title>` 标签文字、顶部导航骨架。

## 五、图片素材
| 文件 | 是什么 |
|---|---|
| `assets/logo.png` | 左上角 LOGO（直接替换同名文件即可） |
| `assets/photos/` 里的 jpg | 牧场相册示例照片（替换/增删） |
| `assets/public-qr.png` | 公网二维码（**不用换**，网址没变就永远有效） |

## 六、改完怎么让所有人看到（更新上线）
1. 改好 `js/data.js` 等文件 → 保存；
2. 回复 AI **"更新上线"**（约 1-2 分钟），AI 会重新部署到永久网址；
3. 网址和二维码**都不变**，别人扫码打开就是新版。

> 💡 小贴士：想先在自己电脑上看效果，可先打开
> `http://127.0.0.1:8123` 预览，确认没问题再"更新上线"。
