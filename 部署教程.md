# 艾牧戈智慧牧场 · 永久在线部署教程（不用开电脑）

> 系统是纯静态网页（HTML/CSS/JS），放到任何云服务器/云存储上，**电脑关机也一直在线**。
> 部署包：`艾牧戈智慧牧场-部署包.zip`（解压后是一个完整网站目录）

---

## 方法一：阿里云/腾讯云 轻量服务器（推荐 · 国内访问最快）

1. 购买一台**轻量应用服务器**（阿里云/腾讯云，2核2G 即可，约 ¥100-300/年，选**呼和浩特/北京/华北**地域离呼伦贝尔近）；
2. 系统选 **Ubuntu / CentOS**，登录服务器终端，把部署包传上去并解压到网站目录：
   ```bash
   # 本机执行（把部署包传到服务器，换成你的服务器IP）
   scp 艾牧戈智慧牧场-部署包.zip root@你的IP:/root/
   # 服务器上执行
   apt update && apt install -y nginx unzip
   rm -rf /var/www/aimuge && mkdir -p /var/www/aimuge
   unzip -o /root/艾牧戈智慧牧场-部署包.zip -d /var/www/aimuge
   chown -R www-data:www-data /var/www/aimuge
   ```
3. 配置 nginx（`/etc/nginx/sites-available/default`）：
   ```nginx
   server {
       listen 80;
       server_name _;
       root /var/www/aimuge;
       index index.html;
       location / { try_files $uri $uri/ /index.html; }
   }
   ```
   ```bash
   nginx -t && systemctl reload nginx
   ```
4. 打开：`http://你的服务器IP`（有域名就绑定域名，国内域名需**ICP备案**）。

> 💡 不想敲命令就用**宝塔面板**：服务器装宝塔 → 网站 → 添加站点 → 上传解压部署包，5 分钟搞定。

---

## 方法二：对象存储静态托管（最省钱 · 几块钱/月）

阿里云 OSS / 腾讯云 COS 都支持静态网站托管，按流量计费，月均几块钱，**默认域名不用备案**：

1. 开通 **阿里云 OSS**（或腾讯云 COS），新建一个 Bucket（地域选呼和浩特/北京）；
2. 权限设为**公共读**；
3. 开启「**静态网站托管**」，默认首页填 `index.html`；
4. 把部署包解压后**全部文件上传**到 Bucket；
5. 打开「静态网站托管」给的访问地址（形如 `https://xxx.oss-cn-huhehaote.aliyuncs.com`）即可访问。

> 想用自己域名（如 ranch.aimuge.com）也可以，绑定后按提示**备案**即可。

---

## 方法三：免费静态托管（零成本 · 国内访问可能慢）

- **GitHub Pages**：注册 GitHub → 新建仓库 → 上传部署包文件 → Settings → Pages → 开启，得到 `https://你的用户名.github.io/仓库名/`；
- **Gitee Pages**（码云，国内）：注册码云 → 新建仓库 → 上传 → 服务 → Gitee Pages 开启（需实名）；
- **Netlify / Vercel / Cloudflare Pages**：拖拽上传部署包即得网址，但服务器在海外，国内访问速度一般。

---

## 部署后要做的 3 件事

1. **换二维码**：部署后把新网址生成二维码（随便一个二维码生成器/微信"码上"），把 `assets/public-qr.png` 替换掉；
2. **HTTPS**：云服务器/托管平台一般自动给 HTTPS，浏览器不会提示不安全；
3. **数据说明**：数据存在每个访问者自己的浏览器里（localStorage），多人各自演示互不影响；正式上线接数据库时可升级。

## 预算对比
| 方案 | 费用 | 电脑开机要求 | 国内访问速度 |
|---|---|---|---|
| 轻量服务器 | ¥100-300/年 | 不需要 | 最快 |
| 对象存储 OSS/COS | 几元/月 | 不需要 | 快 |
| GitHub/Gitee Pages | 免费 | 不需要 | 一般/需实名 |
| 花生壳内网穿透 | 免费~¥100/年 | **需要开机** | 一般 |

> ⚠️ 结论：**只要电脑关机就必须用前三种（云端托管）**；花生壳这类内网穿透只是"远程开电脑"，不算真正永久在线。
